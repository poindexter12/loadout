name: Deploy docs

# Configure Pages to deploy from GitHub Actions. Astro targets the project URL in docs/astro.config.mjs; update it if this repository gains a custom domain.
on:
  push:
    branches: [main]
    paths:
      - docs/**
      - .github/workflows/docs.yml
      # The reference section is generated from these at build time — a plugin
      # release must redeploy the docs or the reference goes stale.
      - plugins/**/.claude-plugin/plugin.json
      - plugins/**/skills/**
      - plugins/**/hooks/hooks.json
      - plugins/**/bin/**
      - .claude-plugin/marketplace.json
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: docs/package-lock.json
      - name: Install documentation dependencies
        working-directory: docs
        run: npm ci
      - name: Build documentation site
        working-directory: docs
        run: npm run build
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
